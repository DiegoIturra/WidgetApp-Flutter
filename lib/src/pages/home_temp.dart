import 'package:flutter/material.dart';

/*Widget que retorna un Center() cuyo child es un texto con el nombre
"home Page"*/
class HomePageTemp extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Home Page"),
    );
  }
}